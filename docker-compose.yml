version: '2'

services:
  front:
    image: rootpia/mnist_front:latest
    container_name: front
    depends_on:
      - ai
    ports:
      - "55000:80"

  ai:
    image: rootpia/mnist_ai:latest
    container_name: ai
